let Promesas = 'Objeto que representa el eventual resultado (o error) de una operación asíncrona. Estas son un objeto de JavaScript que se asocia a una callback function(una función que se ejecuta cuando se completa el proceso asíncrono para procesar el resultado)'

let FunctionCallback = 'Función que se pasa a otra función como argumento y luego se ejecuta dentro de la función externa'

let métodosDeLasPromesas = 'Las promesas tienen un método .then(), con el cual podemos decidir que ocurre cuando se completa la promesa(éxito o error). Además de este método, también existe otro método llamado .catch() el cúal solo se ejecuta si la promesa es rechazada'

/*
Camino y estados de una promesa: 
1: Se hace la promesa
2: Se queda en estado de pendiente (pending)
3: La promesa puede ser cumplida (fulfilled) o rechazada (rejected)
*/

