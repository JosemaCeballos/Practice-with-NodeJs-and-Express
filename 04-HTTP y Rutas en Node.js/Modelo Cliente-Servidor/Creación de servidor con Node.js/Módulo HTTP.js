let MóduloHTTP = 'Este módulo le permite a Node.js trasmitir información con el protocolo HTTP'

//Para llamar a este múdulo escribimos lo siguiente:
const http = require('http')

//Ahora vamos a crear nuestro primer servidor con node en nuestro archivo app.js